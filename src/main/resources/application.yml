server:
  port: 8080

spring:
  datasource:
    # Railway Public URL Bilgileriyle Güncellendi
    url: jdbc:mysql://autorack.proxy.rlwy.net:12051/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "jNomBhSIsaNjnWfGpzBhvIzBUUPebSJL"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # Bağlandığın anda tabloları Railway'de oluşturur
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  sql:
    init:
      mode: never

logging:
  level:
    com.gayrimenkul.system: DEBUG
    org.hibernate.SQL: DEBUG